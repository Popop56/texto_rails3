<% form_for([@texto, @texto.comments.build], :remote => true) do |f| %>
  <%= f.error_messages %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>