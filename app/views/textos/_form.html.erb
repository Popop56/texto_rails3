<% form_for(@textos) do |f| %>
  <%= f.error_messages %>

	<% @textos.each do |texto, index| %>
	  <%= render :partial => 'content_field', :locals => { :texto => texto, :index => index } %>
	<% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
